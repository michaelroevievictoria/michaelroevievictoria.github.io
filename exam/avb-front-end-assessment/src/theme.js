
import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#191970', // Your primary color
    },
    secondary: {
      main: '#ffd700', // Your secondary color
    },
  },
});

export default theme;
